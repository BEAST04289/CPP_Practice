#include <iostream>
using namespace std;

int main() {
cout << " enter a number to check positive or negative: ";
int num;
cin >> num;
cout << (num >=0 ? "positive" : "negative") << endl;
    return 0;
}
